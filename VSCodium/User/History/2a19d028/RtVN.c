#include <stdio.h>

struct employee{
    char name[32];
    char ID[32];
    float salary;
    
};
int main(){
    struct employee e1;
    printf("Enter name: ");
    // fgets(e1, 32, stdin);
    scanf("%s", &e1);
    print
}